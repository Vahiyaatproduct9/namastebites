{
  "name": "server",
  "version": "1.0.50",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build --compile --minify --target bun src/index.ts --outfile server",
    "start": "./server"
  },
  "dependencies": {
    "elysia": "latest",
    "pg": "^8.18.0"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
